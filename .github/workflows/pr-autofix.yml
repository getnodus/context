# PR Auto-Fix — lightweight caller. The heavy logic lives in getnodus/workflow.
# When CI fails or PRs are conflicted, Claude Code auto-repairs them.

name: PR Auto-Fix

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
  schedule:
    - cron: '17 */3 * * *'
  workflow_dispatch: {}

jobs:
  call:
    uses: getnodus/workflow/.github/workflows/pr-autofix.yml@38be6980f5dd3ed24935b483a309206d85c0611f # main
    permissions:
      contents: write
      pull-requests: write
      id-token: write
      actions: read
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
